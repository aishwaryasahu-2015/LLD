LLD for ATM uses
1. State design Pattern: Task to execute only when entity in discussion is in a particular state.
2. Chain of Responsibility : If there are multiple handlers which can serve a particular request in a particular order.
